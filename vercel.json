{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "prisma generate && next build",
  "env": {
    "DATABASE_URL": "file:./dev.db",
    "NODE_ENV": "development"
  }
}